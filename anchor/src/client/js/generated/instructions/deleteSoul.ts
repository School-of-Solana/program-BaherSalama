/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from 'gill';
import { SOULSEND_PROGRAM_ADDRESS } from '../programs';
import { getAccountMetaFactory, type ResolvedAccount } from '../shared';

export const DELETE_SOUL_DISCRIMINATOR = new Uint8Array([
  250, 27, 88, 157, 73, 114, 233, 250,
]);

export function getDeleteSoulDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(DELETE_SOUL_DISCRIMINATOR);
}

export type DeleteSoulInstruction<
  TProgram extends string = typeof SOULSEND_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountSoul extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountSoul extends string
        ? WritableAccount<TAccountSoul>
        : TAccountSoul,
      ...TRemainingAccounts,
    ]
  >;

export type DeleteSoulInstructionData = { discriminator: ReadonlyUint8Array };

export type DeleteSoulInstructionDataArgs = {};

export function getDeleteSoulInstructionDataEncoder(): FixedSizeEncoder<DeleteSoulInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([['discriminator', fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: DELETE_SOUL_DISCRIMINATOR })
  );
}

export function getDeleteSoulInstructionDataDecoder(): FixedSizeDecoder<DeleteSoulInstructionData> {
  return getStructDecoder([
    ['discriminator', fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getDeleteSoulInstructionDataCodec(): FixedSizeCodec<
  DeleteSoulInstructionDataArgs,
  DeleteSoulInstructionData
> {
  return combineCodec(
    getDeleteSoulInstructionDataEncoder(),
    getDeleteSoulInstructionDataDecoder()
  );
}

export type DeleteSoulInput<
  TAccountPayer extends string = string,
  TAccountSoul extends string = string,
> = {
  payer: TransactionSigner<TAccountPayer>;
  soul: Address<TAccountSoul>;
};

export function getDeleteSoulInstruction<
  TAccountPayer extends string,
  TAccountSoul extends string,
  TProgramAddress extends Address = typeof SOULSEND_PROGRAM_ADDRESS,
>(
  input: DeleteSoulInput<TAccountPayer, TAccountSoul>,
  config?: { programAddress?: TProgramAddress }
): DeleteSoulInstruction<TProgramAddress, TAccountPayer, TAccountSoul> {
  // Program address.
  const programAddress = config?.programAddress ?? SOULSEND_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    soul: { value: input.soul ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, 'programId');
  return Object.freeze({
    accounts: [getAccountMeta(accounts.payer), getAccountMeta(accounts.soul)],
    data: getDeleteSoulInstructionDataEncoder().encode({}),
    programAddress,
  } as DeleteSoulInstruction<TProgramAddress, TAccountPayer, TAccountSoul>);
}

export type ParsedDeleteSoulInstruction<
  TProgram extends string = typeof SOULSEND_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    payer: TAccountMetas[0];
    soul: TAccountMetas[1];
  };
  data: DeleteSoulInstructionData;
};

export function parseDeleteSoulInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>
): ParsedDeleteSoulInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error('Not enough accounts');
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { payer: getNextAccount(), soul: getNextAccount() },
    data: getDeleteSoulInstructionDataDecoder().decode(instruction.data),
  };
}
